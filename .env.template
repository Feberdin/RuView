# Purpose: List the local variables required by docker/docker-compose.yml.
# Input: Copy this file to the ignored `.env` file and enter a freshly generated
# value locally. Output: Docker Compose reads the value without committing it.
# Invariant/debugging: Never commit `.env`, log the value, or reuse a production
# credential. Compose fails fast with a clear error while the value is empty.

RUVIEW_API_TOKEN=
