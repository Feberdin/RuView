# wifi-densepose-sensing-server (ruvector-mincut -> ruvector-core / hnsw_rs / mmap-rs):
# The checked-in lockfile and security backports are verified together on this
# exact toolchain. Keep local, Docker, and CI builds aligned to avoid accepting
# a dependency resolution that another environment cannot compile.
[toolchain]
channel = "1.94.0"
profile = "minimal"
